function set_frame_orientation(i,x,y,z,s)
%Mandamos código de grupo de funciones 0x03

%Mandamos código de funcion 0x01

if(i<=0 || i>15)
%error, el marco cero esta reservado y solo puede haber hasta 15 marcos
    

else
    if(1)%sistema derecho valido
        %Mandamos el parametro i

        %Mandamos las componentes de todos los vectores
    else 
        %Mandar mensaje de que el sistema no es derecho
    end


end


end